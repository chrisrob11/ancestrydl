package main

var (
	// Version is set via ldflags during build
	Version = "dev"
	// BuildDate is set via ldflags during build
	BuildDate = "unknown"
)

func main() {
	// CLI application will be implemented here
}
